# Register — reveal-game-show

**Reconstructed from this project's own documents, not authored.** Every row
names the file and line it came from. A row nobody can trace back is a claim,
and a claim written down reads exactly like a recorded fact.

A `Status` of `unknown` means the source said nothing about status. It is a
question for the owner, not a guess.

---

## Decisions (D)

ADR-style: numbered, dated, **immutable once ratified**. A decision is never edited —
it is superseded by a later entry that links back. A rejected decision stays, struck
through, so it is not re-proposed.

**One intake path.** Anything that changes scope enters as a `D<n>`. An item with no
number has not been decided, however clearly it was said aloud.

| ID | Status | Milestone | Decision | Source |
|---|---|---|---|---|
| D1 | ratified | — | Add /tests/ (runner + fixtures) to the §3 repo layout | `docs/plans/v1.0-build-plan.md:146` |
| D2 | ratified | — | Add /docs/runbooks/ | `docs/plans/v1.0-build-plan.md:147` |
| D3 | ratified | — | /vendor/ gains a per-dependency subdirectory (/vendor/reveal.js/) plus a provenance README.md | `docs/plans/v1.0-build-plan.md:148` |
| D4 | ratified | — | Pin reveal.js to 5.2.1, ESM build, four dist/ files only | `docs/plans/v1.0-build-plan.md:149` |
| D5 | ratified | — | Add js/schemas.js to the §3 file list | `docs/plans/v1.0-build-plan.md:150` |
| D6 | ratified | — | State gains "appVersion" alongside schemaVersion | `docs/plans/v1.0-build-plan.md:151` |
| D7 | ratified | — | Document that the app requires an HTTP origin, not file:// | `docs/plans/v1.0-build-plan.md:152` |
| D8 | ratified | — | Add js/app.js (composition root) to the §3 file list | `docs/plans/v1.0-build-plan.md:154` |
| D9 | ratified | — | Add tools/firefox-run-tests.py | `docs/plans/v1.0-build-plan.md:159` |
| D10 | ratified | — | Add docs/plans/theme-contract.md (normative DOM + token contract) and docs/handoffs/ for external collaboration | `docs/plans/v1.0-build-plan.md:155` |
| D11 | ratified | — | Theming is a shared work surface: the maintainer authors CSS/vanilla JS directly, and an external collaborator (Claude Design) contributes t | `docs/plans/v1.0-build-plan.md:156` |
| D12 | ratified | — | Add /games/games.json (game manifest) and a startup game picker; extend §10's v1 feature list with F11 | `docs/plans/v1.0-build-plan.md:157` |
| D13 | ratified | — | A startup theme picker whose choice may override the content file's theme, stored as a device preference outside session state; extend §10's | `docs/plans/v1.0-build-plan.md:158` |
| D14 | ratified | — | **A cell never prints its QUESTION on the face; it may print its ANSWER.** Narrows theme-contract §2's rule that `.qbe-cell-text` and `.qbe-cell-value` are mutually exclusive. A **revealed** `ranked-list` row prints its `answer` AND its points — the one cell carrying both children; a hidden row prints neither. Rejected alternative: a new `.qbe-cell-answer` element, which adds a class no shipped theme styles, where `.qbe-cell-text` is already styled in `default.css` and the ranked-row layout was already `flex-direction: row; justify-content: space-between` — a two-child layout waiting for a second child. Closes `RR5`. **Ratified 2026-09-01 by the maintainer**, on the evidence of the board: a Family Feud round in which the revealed answers do not stay on the screen is not the game. | `docs/plans/theme-contract.md` §2 |
| D15 | ratified | — | **Strikes are modelled in the engine; the steal stays host-mediated.** Spec §10 `F9` scopes feud to a reveal board and says strikes/steals are host-mediated and not modelled, so this contradicts the frozen spec and needs ratification. Scope, settled with the maintainer: the engine owns the strike count and its display, **per column** so a round resets clean; the host still owns who is stealing, whether the steal succeeded, and awarding the pot on the existing `+`/`−` buttons. Rejected: a full steal flow with a round pot, a controlling team and automatic awarding — a wrong state transition mid-show is visible to the room and hard to undo, and the show is in a week. **Ratified 2026-09-01 by the maintainer**, who also fixed `X` as the strike key — widening plan Q12's deliberately small keyboard budget, which is part of this decision rather than an implementation detail. Plan: `docs/plans/F9c.md`. | `docs/plans/F9c.md` |
| D16 | ratified | — | **theme-contract §2 gains `.qbe-strikes` and `.qbe-team-strikes`, §4 their tokens.** Follows from `D15`: strikes need a surface. Two elements rather than one because the maintainer chose both placements — a large centre overlay for the room and per-team marks in the score bar for the host. Absent, never empty, when the game type declares no `strikes` block, matching how `scoring.model: "none"` suppresses the score bar entirely. **Ratified 2026-09-01 by the maintainer.** | `docs/plans/F9c.md` |
| D17 | ratified | — | **A ranked board shows ONE round at a time.** Ratified 2026-09-01 by the maintainer, folded into `F9c` rather than deferred. Today every column renders stacked and every round's question is on screen at once, which spoils the questions and does not resemble the game. The obvious workaround — one file per round — is **disqualifying, not merely clunky**: sessions key on `gameHash` (`js/state.js:90`), so a second file is a second session and team names and scores reset between rounds. So multi-round with a running score requires one file with several columns, which forces this decision. It is also a **prerequisite for `D15`**: strikes are per column, and with every round on screen there is no answer to which round's strikes the centre overlay shows — the board has no notion of a current round today. Scope: draw the active column, persist `currentRound` so a mid-show reload returns to the right round, and a key to advance. Expected to be a `data-` attribute on `.qbe-column` (§3) rather than a new element, so a smaller contract change than `D16`. | `docs/plans/F9c.md` |
| D18 | ratified | — | **A strike belongs to a TEAM within a round, and a strike can be undone.** Ratified 2026-09-02 by the maintainer, on the evidence of the built board: *"we need to pin the strikes to a specific team and we should get an undo button, as we may have accidental strikes."* The host's rule, in their words: click the team name, click `X` or the Strike button, and the strikes appear in the centre and under that team's name. **This SUPERSEDES the resolution in `docs/plans/F9c.md` §3**, which kept strikes on the round alone and drew them beside whichever team was active — chosen to avoid a `(column, team)` matrix. The matrix is now the requirement: `strikes` becomes `{ "<column>": { "<teamIndex>": count } }`, every team row shows its OWN count rather than only the active one, and the centre band shows the team on the board. Two consequences that follow and are part of this decision: a strike with no active team is a **no-op**, because there is no one to charge it to; and `undoStrike` decrements rather than clearing, so a mis-click costs one press. No state schema version bump: `strikes` has never shipped outside this branch, so no released file carries the old shape. | `docs/plans/F9c.md` |
| D19 | ratified | — | **A board editor ships as a separate page, `/editor/`, scoped to feud.** The editor is absent from spec v1 entirely; the only mention in the corpus is one line in the unratified v2 draft calling a builder *"a future candidate"*. Settled with the maintainer before planning: boards authored in it are **downloaded as a file and dropped into `/games/`** — content still loads only from there, so spec §6.3 does not move and there is no second source of board data. Rejected: saving boards to browser storage and offering them in the picker, which works on the live site with no repo access but makes board content a new untrusted path; the maintainer took the smaller delta. Scope is **feud only** — the ranked list is a flat shape and it is the content gap for the show; jeopardy's grid and ladder and bingo's uniform card would each need their own rules and their own delta. The editor imports `validator.js` rather than restating its rules, so "valid in the editor" and "loads in the game" cannot drift apart. **Ratified 2026-09-02 by the maintainer.** Plan: `docs/plans/F13.md`. | `docs/plans/F13.md` |
| D20 | ratified | — | **The startup picker offers a route to the editor.** Ratified 2026-09-03 by the maintainer: *"at the entry point of the game, let's let people choose: start or continue a game OR create a new series of questions."* The editor shipped under `D19` with nothing in the app pointing at it, so a host had to know the URL. Implemented as a real `<a href="editor/">` beside the board list, **not** as a fork screen in front of the picker: the picker is the entry point every game passes through, and an extra step would cost a click on every start, in front of a room, to serve the rarer case. `D19`'s isolation is unaffected — that is about CODE, and a link shares no module; the game still never loads the editor. Contract: theme-contract §2 gains `p.qbe-startup-aside` and `a.qbe-startup-editor`. Noted for later: the maintainer's stated hope is that the editor eventually authors new GAME TYPES, which is a much larger delta and is not this one. | `docs/plans/theme-contract.md` |
| D21 | **proposed — parked, revisit after the tool debuts** | — | **Board generation accepts a spreadsheet, not only a CSV.** Raised by the maintainer 2026-09-11 for the next version: *"a way to create the json from a csv, google sheet, or excel file."* `C20` already covers CSV. The gap is real and was hit twice in one session — a `.numbers` file was saved twice believing it was a CSV, and both times the update silently did not reach the tool. **Evidence run before proposing, per the conventions rule that a claim about another system is not ratifiable until something has run:** `.xlsx` IS readable with Python stdlib alone (`zipfile` + `xml.etree`) — a real file exported from Numbers parsed to 12 columns with no dependency added. Two gotchas found in that run and recorded here so they are not rediscovered later: the sheet carried a **title row above the header**, so row 0 is not the header; and timestamps arrive as **Excel date serials** (`46275.506412037037`), not text. `.numbers` is NOT stdlib-readable — Snappy-compressed protobuf — and the only local route is scripting Numbers itself, which binds the tool to macOS with Numbers installed. Google Sheets has two honest routes: a **published-to-web CSV URL** (no auth, one fetch) or plain File-to-Download-CSV, which the existing path already serves; the API route costs OAuth credentials and is hard to reconcile with zero-cost. **PARKED 2026-09-11 by the maintainer**: *"The editor is for other people. it's not something we even need to plan now. we'll discuss how this works after the debut of the tool."* So the delivery surface is settled — the editor, for other authors, not the CLI — and everything else is deferred until the tool has been used in front of a room. One finding is recorded now because it constrains that surface and will otherwise be rediscovered: **`.numbers` cannot work in the editor at all.** Reading it requires scripting Numbers.app; a web page has no route to it. The editor path can serve `.csv` and `.xlsx`; `.numbers` and a live Sheets fetch cannot live there. Still open when this is picked up: formats, how Sheets is reached, and whether the editorial map stays hand-written or gains an interactive step — that last being the real cost centre, not the file parsing. Adjacent to the v2 draft's *"GitHub-integrated builder (still a future candidate)"* (line 100) | `docs/plans/00-register.md` |
| D22 | ratified | — | **A host can move the board BACKWARD through rounds, and can see which round they are on.** Raised by the maintainer 2026-09-11: *"if someone accidentally skips round 7, and they land on round 8, we would want the ability to go back to round 7 at the state it is at."* `D17` gave a ranked board one round at a time and a control to ADVANCE; there is no way back, and nothing on screen names the current round — which is how a round gets skipped unnoticed in the first place. **Most of this is already built, and that is the point of the delta being small.** `state.setRound` (`js/state.js:996`) already takes any index and clamps to `0..last` — it was never advance-only. Cell keys are `"<column>:<row>"`, so reveals are already column-scoped; strikes are already per round (`D15`); and `repaint` derives the entire board from the session every time, so writing `currentRound` back repaints the earlier round in exactly the state it was left, with NO new persisted state and no schema change. What is missing is only the way to ask: an `onRoundPrev` handler and a control. **Scope settled with the maintainer at the gate:** a `◂ Previous round` button beside `Next round ▸`; a host-facing `Round N of M` indicator in the toolbar; `←`/`→` bound to previous/next, taking the keyboard budget from one key to three; and **scores do NOT rewind** — cells and strikes belong to a round, the score belongs to the game, and a mis-awarded point is corrected with the ± buttons a host already uses. Rejected: wrapping `Next round` at the end, which the existing clamp deliberately refuses because it *"would put a spent round back on the screen in front of the room and read as a reset"*; and rewinding scores, which would need per-round score attribution — new persisted state, a schema change, and a new class of bug. **RATIFIED 2026-09-11 by the maintainer** — *"d22 is approved"* — and built the same session under ultracode as a four-agent workflow. **Contract change:** theme-contract §2 line 241 gains `"round-prev"` to the `data-action` enum, and the toolbar gains a round indicator element. Absent, never inert, on any layout but `ranked-list`, matching how `"round-next"` already behaves. Plan: `docs/plans/F14.md` | `docs/plans/theme-contract.md` |
| D23 | ratified | — | **The board editor prints a host sheet.** Requested by the maintainer 2026-09-12: *"the board editor page is what should be printed. just a print option for that page"* — black text on white, with the questions, the answers and the point values. **RATIFIED 2026-09-12 by the maintainer** — *"build with 2 agents"* — with the open question on invalid boards left unanswered and resolved to the plan's stated default: printing an invalid board is ALLOWED and the sheet is stamped `DRAFT`. Printing is not export (`M25`); a host may legitimately want a draft on paper. **Scoped to the editor, which is what makes it small and safe.** The first reading of the request was a print view of the GAME board, and that is a much bigger and more dangerous thing: `faceText` (`js/renderer.js:294`) returns `null` for a cell in its initial state, so an unrevealed answer is not in the game's DOM at all — `M3` exists to keep it that way. A game-side answer key would have to be rendered fresh AND would create a surface that must never reach the projector. The editor has neither problem: it already shows every answer to the one person looking at it, and it is a separate page that the game never loads (`D19`). **The sheet is built from the editor's MODEL, not scraped from the page, and that is the whole design decision.** Answers may be 2000 characters (`LIMITS.maxAnswerChars`) and they live in `<input>` elements; an input prints only what fits its box, so a long answer is silently truncated on paper with nothing to indicate it. Printing the model instead wraps properly, and it is `D46`'s rule applied: the printed page is a SECOND ARTEFACT, built from the same object the validator judges rather than from a second reading of the DOM. **Scope:** a `Print` button that builds a print-only section from the model and calls `window.print()`; an `@media print` block that forces black-on-white, hides the editing chrome (open row, buttons, tools, verdict, manifest line, lede), and shows the sheet. Feud only, because the editor is feud only (`D19`). No change to the game, to `js/`, or to any theme. **Rejected:** styling the existing `<input>` grid for print, which cannot wrap and so truncates; and a game-side print view, which is `D19`'s separation and `M3`'s guarantee both given up for a document the editor can already produce. Plan: `docs/plans/F15.md` | `docs/plans/F15.md` |

## Risks (RR)

| RR1 | [ci-absent] **Ratified 2026-08-31.** The owner: *"that is a huge error."* No CI. Every check in this project runs only where somebody remembers to run it. | ratified | — | — | Evidence: `ls .github/workflows` → (no workflows) | agent |
| RR2 | [generated-orphan] **Ratified 2026-08-31.** A generated file with nothing regenerating it. It will go stale and be believed, because it does not look stale. | ratified | — | — | Evidence: `grep -l GENERATED docs/**/*.md; ls .github/workflows .githooks` → docs/plans/00-status.md | agent |
| RR3 | [tests-collect-zero] **Ratified 2026-08-31.** Tests exist and the runner collects none of them. A suite that reports green having run nothing is worse than no suite. | ratified | — | — | Evidence: `python3 -m pytest --collect-only -q` → no tests collected in 0.01s | agent |
| RR4 | [test-isolation-theme] The shell suite boots the real app and inherits the browser's real `localStorage`. `withEmptyShelf` clears `qbe.session.*` and nothing else, so the F12 theme override `qbe.theme` survives into the boot under test: with `qbe.theme = marquee` set, the assertion that `themes/midnight.css` loads under `default.css` fails. A test whose result depends on who last used the browser is not evidence in either direction. | ratified | — | — | Extend the shelf helper to save/restore `qbe.theme` alongside the session keys | agent | Evidence: 369/370 in a profile with `qbe.theme = marquee`; 370/370 at `59e2651` on a clean profile |
| RR5 | [feud-face-no-answer] A revealed `ranked-list` row shows only its point value on the board face, never its answer text. `faceValue` (`js/renderer.js:265`) returns `String(cell.value)`, and `.qbe-cell-text` (`js/renderer.js:357`) is emitted only for a cell with no `value` — the bingo shape. A feud cell has `value` and `answer` and no `prompt`, so its answer has no path to the face. The overlay shows it once and closes, leaving the board six blanks and a number, while the accessible name already says "answer shown". | ratified | — | — | Fixed on `F9b/ranked-row-answer`: `faceText()` in `js/renderer.js`, one rule in `themes/default.css`, six assertions and four mutations (`M1`–`M3`, `M5`, `M6`). The theme-contract §2 amendment it depends on is `D14`, ratified 2026-09-01. | agent | Evidence: board face `"38"`, aria `"…answer 1 of 6, 38 points, answer shown"` |
| RR6 | [firefox-runner-contention] `tools/firefox-run-tests.py` deletes and recreates a fixed profile path and binds a fixed Marionette port, so a still-running headless Firefox from an earlier run makes the next one report FALSE geometry failures rather than a harness error — observed as `.qbe-board` resolving 3 grid tracks instead of 5 and a cell measuring 0x0. It reads exactly like a real cross-browser layout regression. Same tree, same commit, after `pkill`: 377/377. | ratified | — | — | Use a unique profile dir and an ephemeral port per run, or refuse to start when the port is already bound | agent | Evidence: 375/377 with a stray instance; 377/377 on the same tree once cleared |
| RR7 | [suite-writes-device-theme] The suite writes the HOST'S REAL device theme preference. `runStartupSuite` sets `qbe.theme` to `chalkboard` and then to the deliberately invalid `nosuchtheme` to prove the fallback, restoring the original in a `finally`. A `finally` does not run when the page is reloaded or closed mid-run — and a suite that takes ~40 seconds gets interrupted often. Observed: a host's browser left holding `qbe.theme = nosuchtheme` after interrupted runs, which silently overrides every board's own theme until someone notices. Not a product defect; a test that can corrupt the state of the machine it runs on. | ratified | — | — | Write the probe values under a test-only key, or restore on `beforeunload` as well as in `finally`, or assert the fallback against `readThemePreference` without ever writing the real key | agent | Evidence: found on the maintainer's own browser 2026-09-02; the value survived because the run was reloaded before its `finally` |
| RR8 | [engine-writes-unreadable-state] The engine can write a session its own validator refuses to read, and the resume shelf offers it anyway. `setTeams` stranding strikes was one instance (`M19`); `addStrike` lacking a COLUMN bound was a second, found the same day by an assertion that revalidated after writing. The class is what matters: `update()`'s `cheapCheck` does not run the validator, so any mutator can produce an unloadable document, and `listSessions`/`summaryOf` read only the title and timestamp, so the row is still offered and dies on click with Discard as the only exit. | closed | — | — | **Fixed.** The shelf stops trusting itself: `app.js` — the one place holding the bundle, the validator and the state layer — runs `validateState` on the single row whose hash could actually be resumed, and hands the renderer a `blockedReason`. A blocked row loses its Resume button and says *this saved game cannot be opened*, with the failing path, so the host is never handed a dead game mid-show and knows it is not something they did. Judging stayed in `validator` and drawing in `renderer`, so no module boundary moved. Rejected: revalidating inside `update()`, which would put `state` across the boundary into `validator` on every keystroke. Locked by `M22`. | agent | Evidence: two instances in one feature; both reproduced end-to-end in the real UI and both now carry a mutation |
| RR9 | [second-artefact-unvalidated] A feature can validate its PRIMARY output and still emit a secondary artefact nobody checks. The editor validated the board with the real validator and then hand-concatenated the `games.json` line it told the host to paste — a line the manifest validator refuses for the repo's own demo board, and not even JSON if the title contained a quote. A manifest has no partial render, so one bad key takes the picker down for every board. The class: wherever we generate something a human is told to paste, that artefact needs the same validator the primary output gets. | ratified | — | — | Validate every generated artefact through the schema that will consume it, and assert on the artefact rather than on the model that produced it | agent | Evidence: reproduced with `demo-feud-rounds.json`, whose title is a legal title and an illegal manifest key |
| RR10 | [assertion-cannot-fail] Three assertions in two days passed while unable to detect the behaviour they named: `M12`'s size check sat in the per-game block instead of the per-theme loop; `M24`'s round-trip re-derived the bytes it expected instead of reading the download path, twice; `M28`'s manifest check used a title that was accidentally a valid key. Each was written in good faith and each was proven toothless only by mutating the shipped code. | ratified | — | — | For every new assertion, name the mutation that kills it and RUN it before recording the row as closed — the register's "caught at N/M" column is the evidence, and a row without one is a claim | agent | Evidence: `M12`, `M20`, `M24` (x2), `M28` |

| ID | Risk | Status | Likelihood | Impact | Mitigation | Owner | Source |
|---|---|---|---|---|---|---|---|

*(none found in this project's documents)*

## Owner items (O)

Outstanding items only the owner can close.

| ID | Item | P | Status | Source |
|---|---|---|---|---|

*(none found in this project's documents)*

## Chores (C)

`conventions.md`: *a chore gets a file only when it has tasks and a verification
step; a one-line chore lives in the register.* This is that register. A chore with
its own plan file is listed here too, with a link, so one read gives all of them —
the absence of that read is how a chore killed by a ratified delta stayed open for
days on the owner's track.

| ID | Chore | P | Status | Owner | Plan | Source | TD |
|---|---|---|---|---|---|---|---|
| C1 | git init, main branch, identity configured | — | done | — | — | `docs/plans/v1.0-build-plan.md:256` |
| C2 | Directory scaffold per §3 + D1/D2 | — | done | — | — | `docs/plans/v1.0-build-plan.md:257` |
| C3 | AGPL-3.0 LICENSE at repo root | — | done | — | — | `docs/plans/v1.0-build-plan.md:258` |
| C4 | Vendor reveal.js 5.2.1 with provenance + hashes (vendor/README.md) | — | done | — | — | `docs/plans/v1.0-build-plan.md:259` |
| C5 | .gitignore | — | done | — | — | `docs/plans/v1.0-build-plan.md:260` |
| C6 | CLAUDE.md — project rules, the named invariants, work vocabulary | — | done — CLAUDE.md exists | — | — | `docs/plans/v1.0-build-plan.md:261` |
| C7 | Create public GitHub repo quiz-board-engine, push main | — | done — repo is public and pushed | — | — | `docs/plans/v1.0-build-plan.md:262` |
| C8 | Enable GitHub Pages (deploy from main, root) | — | done — GitHub Pages status: built | — | — | `docs/plans/v1.0-build-plan.md:263` |
| C9 | README — tokens, limits, ?game=, "Fixing JSON errors", local-server note, theme-trust warning | — | done | — | — | `docs/plans/v1.0-build-plan.md:264` |
| C10 | Safari manual test runbook | — | done | — | — | `docs/plans/v1.0-build-plan.md:265` |
| C11 | Safari manual test pass — human, maintainer-executed | — | open | — | — | `docs/plans/v1.0-build-plan.md:266` | td:6hPqwQfrR8QJW5fw |
| C12 | Demo content authoring (games/demo.json + one per game type) | — | done — 4 game json files present | — | — | `docs/plans/v1.0-build-plan.md:267` |
| C13 | docs/runbooks/learning-track.md — the ongoing 10% study plan | — | done | — | — | `docs/plans/v1.0-build-plan.md:268` |
| C14 | docs/runbooks/v1.0-maintainer-pass.md — the maintainer's four-hour list to v1.0 | — | done | — | — | `docs/plans/v1.0-build-plan.md:269` |
| C15 | Add CI — build, the Safari gate, and whatever the suite becomes | P0 | open | Marty | — | closes `RR1` |
| C16 | Regenerate `docs/plans/00-status.md` from this repo, or stop generating it here | P1 | open | Marty | — | closes `RR2` |
| C17 | Make the test suite collect — it currently reports green having run nothing | P0 | open | Marty | — | closes `RR3` |
| C18 | Track the v2.0 draft spec in git — `docs/specs/quiz-board-engine-spec-v2-draft-r2.md`. Draft, not frozen: v1 remains the contract until the §8 red-team gate resolves. | P2 | done | agent | — | — |
| C19 | `docs/pr-review-log.md` — one entry per merged PR, committed to `main` after the merge. Opened with the PR #1 entry. | P2 | done | agent | — | — |
| C20 | Survey CSV → board generator (post-review). `tools/csv-to-board.py` + `tools/validate-board.mjs` + a hand-maintained map holding the editorial half (merges, drops, labels). Generates the board AND its `games.json` entry, both validated by the engine's own validator. | P2 | built — 60/60, `M29`–`M41` run; adversarial review's 5 majors all fixed | agent | `docs/plans/C20.md` | — |

## Gates (G)

A gate is an **event**, not a place — but the event has to be recorded somewhere or it
exists only in the conversation where it happened. It is not inferable from the
filesystem: the first attempt flagged six features as awaiting a gate and all six were
already built.

| ID | Gate | Status | Plan written | Crossed | What the owner said | Source |
|---|---|---|---|---|---|---|

*(none found in this project's documents)*

## Hooks (H)

Deterministic enforcement. Prose is advisory; hooks are not. An `H<n>` is a plan-local
label and **never appears in a commit, branch, or PR title**.

| ID | Hook | Surface | Protects | Status | Source |
|---|---|---|---|---|---|

*(none found in this project's documents)*

## Mutations (M)

Named ways to break the code, each paired with the test that must catch it.
A test is not evidence until a mutation proves it can fail.

| ID | File | Mutation | Caught by | Status | Source |
|---|---|---|---|---|---|
| M1 | `js/renderer.js` | `faceText` returns `null` for a revealed ranked row — the answer never reaches the board face. This is the shipped defect, reproduced deliberately. | `a revealed ranked row keeps its ANSWER on the board face, not just in the overlay` (and the ordering assertion, incidentally) | closed — caught, 371/373 | `RR5`, `D14` |
| M2 | `js/renderer.js` | `buildCell` appends `.qbe-cell-value` before `.qbe-cell-text` — points at the left of the row, answer at the right. | `a ranked row BUILT revealed orders answer before points` + the §2 contract checker, on a RESUMED board | closed — **survived at first**, 373/373 green; a row is built hidden and gains both children on reveal, so nothing exercised `buildCell`'s order. Two assertions and a second `assertContract` call were added on the resume path, and M2 then failed 375/377. | `D14` |
| M2b | `js/renderer.js` | `updateBoard` inserts the value at `firstChild` instead of after the text — same swap, on the reveal path rather than the build path. | `a revealed ranked row emits the answer BEFORE the points (left, then right)` | closed — caught, 372/373 | `D14` |
| M3 | `js/renderer.js` | `faceText` drops its `initialState` guard — every hidden row prints its answer, giving the whole board away before a single reveal. | `a hidden ranked row prints no answer either, so nothing is given away` | closed — caught, 376/377, reporting the leaked answer by name ("Their phone") | `D14` |
| M5 | `themes/default.css` | Delete `.qbe-board[data-layout="ranked-list"] .qbe-cell-value { margin-inline-start: auto }` — the rule that pins the points to the end of the row. | `a revealed ranked row pins its points to the right edge of the row` (shell suite, measured inside the booted iframe where theme CSS is actually loaded) | closed — **survived the first fix**, 377/377 green, found by adversarial review rather than by the suite: every render assertion draws into `harnessStage()`, which carries no stylesheet, so nothing could see a layout. With the new shell assertion M5 fails 377/378, reporting the points 423px from the right edge instead of 29px. | `D14` |
| M6 | `js/renderer.js` | `faceText` guards `cell.answer === undefined` instead of truthiness, so a valid `"answer": ""` puts an EMPTY `.qbe-cell-text` on the face. | `a revealed ranked row with an EMPTY answer omits .qbe-cell-text entirely` + the §2 contract checker on the same board | closed — caught, 379/381. Found by adversarial review; the suite had no fixture with an empty required string anywhere, and the §2 checker counts cell children without reading them. | `D14` |
| M7 | `js/state.js` | `addStrike` does not cap at the game type's `strikes.count` — a fourth strike is recordable. | `strikes cap at the game type's own count, in the SESSION not just on screen` | closed — caught, 402/405 | `D15` |
| M8 | `js/state.js` | Strikes key off the board rather than the column, so Round 2 inherits Round 1's strikes. | `strikes are per round — a second round starts from its own count` | closed — caught, 402/405 | `D15` |
| M9 | `js/schemas.js` | An imported `"strikes": {"0": 99}` is accepted unvalidated and renders 99 marks. | `an imported session with more strikes than the game type allows is refused` | closed — **the original row was wrong twice over**, and adversarial review caught both. The mutation as planned (`{"0": 99}`) is refused by the STRUCTURAL `max: 10` and never reaches this cross-check at all; 4 is the smallest value only it can reject. And the named catcher was a code path, not a test — see `M16`, which deleted the rule with the suite still green | `D15` |
| M10 | `js/renderer.js` | `.qbe-strikes` renders for a game type that declares no `strikes` block. | `a game type that declares no strikes draws no strike band at all` | closed — caught, 402/405, twice: also by the pre-existing `hostile prompt/answer/label add ZERO element nodes` node count | `D16` |
| M11 | `js/renderer.js` | The centre overlay draws marks with no accessible name, so the count is invisible to a screen reader. | `the strike band announces the count, and the marks themselves are decoration` | closed — assertion written; the band's name is the count and the marks are `aria-hidden` | `D16` |
| M12 | `themes/*.css` | The marks are styled only in `default.css`, so a themed board shows nothing. | `a strike mark is drawn large enough to read from the back of a room` | closed — **SURVIVED the first time**, 404/404 green while a mark rendered 4x4px. The assertion that closed it was then still in the wrong place: the plan named the PER-THEME loop and it went into the per-GAME block, measuring one theme and only its size. `M15` covers what this row was always reaching for | `D16` |
| M13 | `js/state.js` | `currentRound` is held in view state instead of the session, so a mid-show reload returns to Round 1 with the scores intact — the failure a host would notice only on stage. | `a board rebuilt from a session opens on the round it was left on` | closed — caught, 390/391 | `D17` |
| M14 | `js/renderer.js` | Non-active rounds are hidden with CSS alone, so their cells stay in the accessibility tree and reachable by `Tab` — a keyboard host can open a cell in a round the room cannot see. | six assertions, including the pre-existing no-inline-style invariant | closed — caught, 385/391; the CSS-only hide was also caught independently by `renderer.js sets no inline style but the --qbe-column-count property` | `D17` |
| M15 | `themes/*.css` | Every theme takes the base layer's `--strike-color: #a4243b` — one red tuned for a light board, applied to four dark ones. This is what F9c actually SHIPPED. | `theme "<name>": the strike marks meet WCAG 2.1 AA contrast`, computed per theme from the two files' cascade | closed — caught, 412/415, reproducing adversarial review's numbers exactly: 2.74:1 midnight, 1.63:1 chalkboard, 2.45:1 marquee. `games/demo-feud.json` ships on midnight | `D16` |
| M16 | `js/validator.js` | Both new cross-checks disabled outright — the strikes loop replaced with `for (const key of [])` and the `currentRound` check prefixed `false &&`. | five assertions driving `validator.validateState` against real bundles | closed — caught, 410/415. **This exact mutation left the suite green at 405/405** before the assertions existed: two validator rules were deletable whole while the register recorded them covered | `D15`, `D17` |
| M17 | `js/state.js` | `addStrike` falls back to team 0 when no team is active, instead of doing nothing — so "pinned to a specific team" quietly stops meaning anything and a stray `X` charges whoever is first in the list. | `a strike with no active team is a no-op, not a strike against team 0`, plus two more | closed — caught, 419/422, three ways | `D18` |
| M18 | `js/state.js` | `undoStrike` deletes the team's entry instead of decrementing, so one press wipes three strikes. | `undo takes back exactly one strike, not the whole row` | closed — caught, 421/422. The mutation is the failure mode the undo exists to prevent, inverted: a one-key slip becomes a three-key one in the other direction, in front of the room | `D18` |
| M19 | `js/state.js` | `setTeams` stops carrying strikes, so removing a team strands its entry — **the shipped critical**. | `removing a team drops its strikes, so the session it writes still loads`, which REVALIDATES the session rather than inspecting it | closed — caught, 429/430, naming `strikes["0"]["2"]` | `D18` |
| M20 | `js/renderer.js` | Every team row paints team 0's count, so "pinned to a team" is invisible on screen. | `each team row draws ITS OWN strike count` + the accessible-name assertion | closed — caught, 428/430, two ways. **This mutation survived at 422/422 before review**: every `D18` assertion lived in the state group and none reached the renderer | `D18` |
| M21 | `js/renderer.js` | The undo control is never rendered. | `the toolbar carries an undo control when the game type has strikes` | closed — caught, 428/430. It first made the NEXT assertion dereference null, killing the runner and showing as a hang; the assertion is now guarded, because a mutation that kills the report proves nothing | `D18` |
| M22 | `js/renderer.js` | The resume shelf goes back to deciding a row is resumable from its game hash alone, ignoring `blockedReason` — so a session the validator refuses is offered again. | `a saved session that will not load offers no Resume button` | closed — caught, 432/433, reporting the buttons drawn | `RR8` |
| M23 | `editor/editor.js` | `judge` stops calling `validateBundle` and applies rules of its own, letting a 13-round board through. | `a board over the column cap is refused by the validator, not waved through` + the same-words assertion | closed — caught, 442/444, reporting *ACCEPTED 13 rounds against a cap of 12*. The mutation IS the design failure the editor exists to avoid: a second copy of the rules | `D19` |
| M24 | `editor/editor.js` | The Download click hands the Blob bytes that were never judged — a literal string. | `pressing Download hands over exactly the bytes that were judged`, which presses the REAL button and captures inside `download()` | closed — caught at 446/447, **on the third attempt**. Recorded first as *"prevented by construction"*, which was false twice over: `toFileText()` was called in two places, and the two assertions that replaced that claim both re-derived the expected bytes and so could not see the click handler at all. Review applied the mutation and got 447/447 green; so did my first fix | `D19` |
| M25 | `editor/editor.js` | Export stays enabled while the board is invalid. | `an incomplete board cannot be exported, and the control says why`, asserted on the CONTROL not the model | closed — caught, 443/444 | `D19` |
| M26 | `editor/editor.js` | A filename `games.json` cannot hold is accepted. | `only a filename games.json could actually hold is accepted` | closed — caught, 443/444, naming `../evil.json`, `notes.txt`, `has space.json`, `games/x.json` | `D19` |
| M27 | `editor/editor.js` | The editor reaches the DOM through `innerHTML`. | the transitive source audit, with `editor/index.html` added as an ENTRY point | closed — caught, 443/444: *editor/editor.js uses no forbidden DOM or dynamic-code API*. This is the one that proves a named invariant reaches a NEW directory rather than quietly stopping at `js/` | `D19` |
| M28 | `editor/editor.js` | The `games.json` line goes back to concatenating the board TITLE, unescaped — **the shipped critical**. | `the games.json line it tells you to paste is itself a valid manifest entry`, which builds a manifest FROM the line and validates it | closed — caught at 446/447 reporting `games["Quiz Board Engine Demo — Three Rounds"]` REFUSED. Survived the assertion's first version, which set the title and the picker name to the same ASCII string and so could not tell them apart | `D19` |
| M29 | `tools/csv-to-board.py` | The unmapped-answer gate is removed from `tally`, so a raw response the map has never seen is silently dropped from the board. This is the gate that makes the tool safe to re-run on a survey that has grown. | `an answer in neither "from" nor "drop" fails the run` + the assertion that the failure NAMES it | closed — caught, 22/24, naming `'49 the spine'` | `C20` |
| M30 | `tools/csv-to-board.py` | `apportion` skips the remainder round — plain floor. This is the hand-entry defect reproduced: the original board's columns total 88, 89, 95, 95, 99, 100, 100. | `every column's point values sum to exactly 100`, and the same assertion restricted to a column that DROPS an answer | closed — caught, 21/24, columns back at 96/97/97/98/98/99/99/99 | `C20` |
| M31 | `tools/csv-to-board.py` | The `games.json` entry is keyed on the board `title` instead of the map's `pickerKey`. | `the manifest entry is keyed on pickerKey, asserted where it DIFFERS from title`, plus the hostile-title assertion | closed — caught, 23/25. **The first version of the assertion could not fail**: the shipped map sets `title` == `pickerKey` == `"Revenue Feud"`, so both implementations produce identical output. That is `M28`'s own recorded failure mode recurring in a new tool; the assertion now builds a fixture where the two differ. The hostile-title assertion fired independently, reproducing `M28` exactly — `games["Revenue "Feud" — \ / <script>` REFUSED | `C20`, `M28` |
| M32 | `tools/csv-to-board.py` | The call to `tools/validate-board.mjs` is deleted, so the tool writes a board the engine would refuse — the second copy of the rules that `M23` exists to prevent, arriving by omission rather than by restatement. | `the generator REFUSES to write a board the engine would reject` + three more, driven THROUGH the generator with an over-cap map | closed — **SURVIVED at 25/25 green.** The original assertion drove `validate-board.mjs` directly and confirmed the CLI refuses 16 columns, which is true and tests the CLI, not the tool; nothing asked whether the generator consults it at all. Rewritten to read the generator's exit code, its stderr, and the absence of BOTH output files: M32 then fails 24/28 | `C20`, `M23` |
| M33 | `tools/csv-to-board.py` | `build_column` ignores the map's `drop` list, so every long-tail answer reaches the board. | `the board carries exactly the map's answers, in order`, asserted as the whole SET per column rather than as one absent name | closed — caught, 20/25, five columns at once, printing `Investments`, `Buses are for commoners.`, `62`, `11` and `Too damn much` | `C20` |
| M33b | `tools/csv-to-board.py` | The apportionment basis silently changes which responses it divides by. | — | **corrected, and it was recorded wrong.** The row claimed a fixture "where the readings DIFFER" between counting blanks in the denominator and not. No such fixture exists: the basis is `sum(counts)` over KEPT answers and a blank can never enter `counts`, so there is no implementation in which blanks change the share. Its own evidence column said *"caught by `M30` and `M33` both"* — it was never seen to fail against a code state where only it fails, which is the "assertion that cannot fail alone" pattern. Found by adversarial review | `C20` |
| M34 | `tools/csv-to-board.py` | `build_manifest` keeps only the board it is writing, so every other entry is dropped and every other board becomes invisible in the picker. | `every board already in games.json survives regeneration, value intact` + the order assertion | closed — caught, 56/58. **Survived at 28/28 before review**: the manifest assertions asked only "is it valid" and "is MY key present", both true of the wiping implementation. The one catastrophic behaviour of the function this chore exists for was the one nothing asserted | `C20` |
| M35 | `tools/csv-to-board.py` | `check_columns` stops reporting unaccounted columns, so a survey that GAINS a question silently drops it. | `a NEW CSV column fails the run rather than being silently ignored` | closed — caught, 56/58. Only the *missing*-column half of `check_columns` had a fixture; the half that matters when a Google Form is edited had none | `C20` |
| M36 | `tools/csv-to-board.py` | The gate reverts from `bundle` to `content` — structural validation only. | `a board whose theme does not resolve is REFUSED, not written`, and the same for `gameType` | closed — caught, 54/58. **This was the shipped state.** `validateDocument` checks shapes; the theme-is-in-themes.json and gameType-config-exists checks live in `checkCrossReferences`, reachable only from `validateBundle`. A board with `"theme": "marque"` passed every gate this tool had and does not load. The plan's claim that the tool "cannot disagree with the engine about what loads" was false as written | `C20`, `D46` |
| M37 | `tools/csv-to-board.py` | Cell answers are built from the raw CSV response instead of the map's label, putting respondent wording on the board. | `no raw response text reaches it either — labels come from the MAP` + the answer-set assertion | closed — caught, 47/58. **The PII assertion it replaced could not fail**: `'@' not in board` is true by construction, since answers come from map labels and the email column is ignored. Review applied this mutation, got raw respondent text onto the board, and the PII assertion stayed green | `C20` |
| M38 | `tools/csv-to-board.py` | `safe_filename` is removed, so the map's `filename` is used as a write path unchecked. | `filename '<path>' is refused` (five paths) + `the file it aimed at is untouched` | closed — caught, 57/58. Review demonstrated a map writing OUTSIDE the repository and outside the temp directory, before validation ran and with no cleanup — `os.path.join` discards everything left of an absolute path. `CLAUDE.md` gives `?game=` an explicit no-traversal rule; this field had none | `C20` |
| M39 | `tools/csv-to-board.py` | The rollback in `land()` is removed, so a failed manifest write leaves the board on disk. | `and leaves NO board file behind — not a valid, unreachable one` | closed — caught, 58/59. **The assertion's first fixture could not reach it**: making the manifest path a directory fails in `build_manifest`, before anything is written. A real manifest in a read-only DIRECTORY is the fixture that separates read failure from write failure. The mutation produces exactly the defect C20 was written to prevent — a valid, served, unreachable board | `C20` |
| M40 | `games/games.json` | A picker key is declared twice. | `games.json declares no key twice — last-wins hides this from both validators` | closed — **found in the shipped file, not by a deliberate mutation.** `games.json` reached `main` with `"Revenue Feud"` listed twice and BOTH validators called it valid: `JSON.parse` and `json.load` each take last-wins silently, so the duplicate is gone before any schema sees the object. The shape is legal and the file is wrong; only the raw text knows | `C20` |
| M41 | `tools/csv-to-board.py` | The `counts[i] > 0` guard is removed, so a zero-response answer may collect a remainder point. | — | **open — mechanism real, trigger UNREACHABLE.** 300 000 random cases show guarded and unguarded apportionment differing in 0. `short` equals the sum of the fractional parts, so it is strictly less than the number of answers HAVING a fractional part, and a zero-count answer sorts last on remainder — it is never inside the first `short` positions. The guard is defensive dead code. Recorded rather than closed, because a mutation nothing can trigger is not evidence of a test | `C20` |
| M42 | `js/app.js` | `onRoundPrev` calls `setRound(round + 1)` — the button advances instead of going back. | `Previous round goes BACK a round, not forward (from Round 2 to Round 1)` | closed — caught, 469/476. Driven from Round 2 on purpose: from Round 1 the clamp hides the bug and the fixture would satisfy both implementations | `D22` |
| M43 | `js/state.js` | A backward move deletes the destination round's `cellStates`, so a returned-to round comes back blank — the exact thing the delta exists to prevent. | `a returned-to round keeps every reveal it had, and nothing else` | closed — caught, 475/476, exactly one row. The cell was revealed through the REAL overlay and read back off the board against the game file's own answer text, not recomputed | `D22` |
| M44 | `js/app.js` | Per-team strikes are computed with `round + 1`, so a returned-to round shows the strikes of the round just left. | `a returned-to round shows ITS OWN strikes, not the later round's` | closed — caught, 472/476. Round 1 holds two strikes and Round 2 holds one, so the counts differ and neither reading can pass for the other | `D22`, `D15` |
| M45 | `js/renderer.js` | `setRoundIndicator` drops its `+ 1`, so the first round reads "Round 0 of 3" — the array index leaking into the one place it is not an index. | `the indicator is ONE-BASED and names the total` + the cold-boot row | closed — caught, 470/476, two ways. Asserted at the FIRST round deliberately: at round index 2 the buggy reading is "Round 2 of 3", which is wrong but plausible, and a fixture both implementations satisfy distinguishes nothing | `D22` |
| M46 | `js/app.js` | The indicator is written once at build behind a one-shot flag instead of on every repaint, so it freezes. | `the indicator follows the board after a Previous as well as a Next` | closed — caught, 473/476. Asserted after a next, a prev AND a second next, so a value written once cannot survive it. A frozen indicator is worse than none: it does not fail to report a skip, it denies one happened | `D22` |
| M47 | `js/renderer.js` | The layout gate is removed and round chrome renders on boards with no rounds. | `a toolbar with no round handlers draws NO round chrome at all — absent, not inert`, plus real boots of `demo-bingo.json` and `demo-feud.json` | closed — caught, 473/476, three times. A positive control (the three-round board really does draw all three) is a separate passing row, so the absence rows cannot pass by nothing ever being drawn. **The first attempt CRASHED the runner** — a comment placed mid-call swallowed the arguments — and was rewritten, because a syntax kill proves nothing (`M21`) | `D22` |
| M48 | `js/app.js` | The arrow handler drops its `ctx.board.open` / `ctx.screen` guards, so an arrow moves the board while the question overlay is up in front of the room. | `an arrow with the question overlay up moves nothing` + `an arrow with the Teams… screen up moves nothing` | closed — caught, 474/476. ArrowRight from Round 1, not ArrowLeft: ArrowLeft there is clamped and would hold still whether the guard exists or not | `D22` |
| M49 | `js/app.js` | The arrow handler drops `event.repeat`, so a held arrow walks the whole game. | `an auto-repeating arrow moves nothing, while the same key pressed once moves one round` | closed — caught, 475/476. The repeat presses and an ordinary press are in ONE assertion, so the guard half cannot pass because the key never arrived — the failure mode `M24` is recorded for | `D22` |
| M50 | `js/renderer.js`, `js/app.js` | `focusRoundCell` returns early, so an arrow that changes round drops a focused cell to `<body>`; and its converse, the repair firing on a CLAMPED arrow that changed nothing. | `an arrow that changes round carries focus onto the new round, not to <body>` and `a CLAMPED arrow leaves focus exactly where it was` | closed — caught at 477/479 each, one row apiece. **Both halves were escapes.** The repair shipped with NOTHING covering it: review made `focusRoundCell` return false and the suite stayed GREEN at 476/476, because no assertion anywhere read `document.activeElement` after a round change. And the repair itself was wrong at the ends — it fired on a no-op move, relocating a host who had tabbed to the last answer, which is the very failure it exists to prevent. A boundary fixture is the only kind that separates "repair always" from "repair only on a real move" | `D22` |
| M51 | `js/app.js` | `setRoundEnabled(true, true)` — the round controls never disable, so `Previous round` is enabled and dead on every fresh board. And its converse, `(false, false)`. | `the round controls disable at the ends and are live in between — no dead button` | closed — caught at 478/479 and 470/479. **Survived its first run**: disabling was implemented and nothing checked it. Asserted at both ends AND in the middle, because disabling everything forever is the other way to make a dead button. The defect was found by review CLICKING the control rather than reading it — the repo states the rule against it twice, once three lines from the new code | `D22`, `D17` |


-----

#AI/Claude
