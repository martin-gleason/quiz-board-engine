# Conventions — reveal-game-show, local

Rules true of **this project only**. The inherited baseline is the pinned
copy in `docs/conventions.md`, which is never edited (`D24`).

Carried here automatically when the project was vendored, so nothing was
lost. Anything below that merely restates the baseline in older words
should be deleted rather than kept — two vocabularies for one rule is how
the corpus reached five incompatible shapes of it.

---

## Licensing

- AGPL-3.0-or-later. Every source file carries `SPDX-License-Identifier: AGPL-3.0-or-later`.
- Vendored third-party code keeps its own license file, unmodified, and is recorded with
  version + source URL + SHA-256 in `vendor/README.md`.

-----
2026-08-16

#AI/Claude
