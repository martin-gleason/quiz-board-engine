.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
node_modules/

# Completed manual-test transcripts. These are pasted terminal output and have been found to
# contain a home-directory listing and a hostname; this repo is public and CLAUDE.md forbids
# identifiable data in it, ever. Keep the evidence locally, or scrub it before tracking it.
docs/handoffs/Runbook*Safari*.md

# Survey response exports. The tracked copies under examples/ are REDACTED: the
# "Have feedback? Drop your email in" column is blanked in every row, because raw form
# responses carry respondent-supplied addresses and this repository is public — CLAUDE.md:
# "no identifiable or regulated data lives here, ever." Redacting that column changes no
# board and no assertion: the map already lists it in `ignoreColumns`, so it has never
# reached a generated file. Maintainer's call, C21.
#
# The UNREDACTED originals stay here and are never committed.
examples/.originals/

# Python bytecode from tools/
__pycache__/

# Spreadsheet originals of survey exports. Same reason as examples/*.csv: these carry
# respondent-supplied email addresses and this repository is public. Chore C20.
*.numbers
*.xlsx
